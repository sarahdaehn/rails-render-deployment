databases:
  - name: postgres
    ipAllowlist: []

services:
  - type: web
    name: rails
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: bundle exec rails s 
    envVars:
      - key: RAILS_MASTER_KEY
        sync: false
      - key: DATABADE_URL 
        fromDatabase:
          name: postgres
          property: commectionString
  - type: redis
    name: redis
    ipAllowlist: []
    plan: free
    maxmemoryPolicy: noeviction

